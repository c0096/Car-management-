<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Commandes</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <header class="topbar">
        <a class="brand" asp-controller="Orders" asp-action="Index">Commandes</a>
        <nav class="nav-actions">
            @if (User.Identity?.IsAuthenticated == true)
            {
                <span class="user-pill">@User.Identity.Name</span>
                <a asp-controller="Orders" asp-action="Index">Commandes</a>
                <a asp-controller="Products" asp-action="Index">Produits</a>
                <a class="button button-primary" asp-controller="Orders" asp-action="Create">Nouvelle commande</a>
                <form asp-controller="Account" asp-action="Logout" method="post" class="logout-form">
                    <button class="button" type="submit">Déconnexion</button>
                </form>
            }
        </nav>
    </header>
    <main class="container">
        @RenderBody()
    </main>
    <script src="~/js/validation.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
